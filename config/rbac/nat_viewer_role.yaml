# permissions for end users to view nats.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: nat-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: cplane
    app.kubernetes.io/part-of: cplane
    app.kubernetes.io/managed-by: kustomize
  name: nat-viewer-role
rules:
- apiGroups:
  - mfplane.mfplane.io
  resources:
  - nats
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - mfplane.mfplane.io
  resources:
  - nats/status
  verbs:
  - get
